{
  "name": "be_portfolio",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "be_portfolio/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/be_portfolio",
        "main": "be_portfolio/src/main.ts",
        "tsConfig": "be_portfolio/tsconfig.app.json",
        "assets": ["be_portfolio/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "be_portfolio/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "be_portfolio:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "be_portfolio:build:development"
        },
        "production": {
          "buildTarget": "be_portfolio:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["be_portfolio/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "be_portfolio/jest.config.ts"
      }
    }
  },
  "tags": []
}
